---
import { nav, org, upcomingQuads } from "@/data/site";
import MobileNav from "@/components/MobileNav.astro";
import Bolt from "@/components/Bolt.astro";

const nextQuad = upcomingQuads[0];
const path = Astro.url.pathname.replace(/\/$/, "") || "/";
const isActive = (href: string) =>
  href === "/" ? path === "/" : path === href || path.startsWith(href + "/");
---

<header
  class="sticky top-0 z-50 border-b border-border bg-background"
>
  <div class="mx-auto flex h-16 max-w-6xl items-center gap-6 px-5">
    <a href="/" class="flex shrink-0 items-center gap-2.5">
      <img
        src={org.logo}
        alt=""
        width="34"
        height="34"
        class="rounded-sm bg-board p-1"
      />
      <span class="leading-none">
        <span class="block font-heading text-[1.02rem] font-semibold tracking-tight"
          >Lightning Square</span
        >
        <span class="label text-[0.62rem]">Chess Club</span>
      </span>
    </a>

    <nav class="ml-auto hidden items-center gap-0.5 lg:flex" aria-label="Primary">
      {
        nav.map((item) =>
          item.children ? (
            <div class="group relative">
              <a
                href={item.href}
                aria-current={isActive(item.href) ? "page" : undefined}
                class:list={[
                  "flex items-center gap-1 rounded-sm px-3 py-2 text-[0.9rem] font-medium transition-colors",
                  isActive(item.href)
                    ? "text-primary"
                    : "text-muted-foreground hover:text-foreground",
                ]}
              >
                {item.label}
                <svg
                  width="11"
                  height="11"
                  viewBox="0 0 24 24"
                  fill="none"
                  stroke="currentColor"
                  stroke-width="2.5"
                  stroke-linecap="round"
                  stroke-linejoin="round"
                  aria-hidden="true"
                  class="transition-transform group-hover:rotate-180"
                >
                  <path d="m6 9 6 6 6-6" />
                </svg>
              </a>
              <div
                class="invisible absolute left-0 top-full w-72 translate-y-1 pt-2 opacity-0 transition-all group-hover:visible group-hover:translate-y-0 group-hover:opacity-100 group-focus-within:visible group-focus-within:translate-y-0 group-focus-within:opacity-100"
              >
                <div class="rounded-md border border-border bg-popover p-1 shadow-lg">
                  {item.children.map((c) => (
                    <a
                      href={c.href}
                      class="block rounded-sm px-3 py-2.5 transition-colors hover:bg-accent"
                    >
                      <span class="block text-[0.88rem] font-medium">{c.label}</span>
                      <span class="block text-[0.78rem] text-muted-foreground">
                        {c.note}
                      </span>
                    </a>
                  ))}
                </div>
              </div>
            </div>
          ) : (
            <a
              href={item.href}
              aria-current={isActive(item.href) ? "page" : undefined}
              class:list={[
                "rounded-sm px-3 py-2 text-[0.9rem] font-medium transition-colors",
                isActive(item.href)
                  ? "text-primary"
                  : "text-muted-foreground hover:text-foreground",
              ]}
            >
              {item.label}
            </a>
          )
        )
      }
    </nav>

    <div class="ml-auto flex items-center gap-2 lg:ml-0">
      {/* The quad FAQ answers most of the questions we get, so it sits in the
          top right corner where help links are looked for. */}
      <a
        href="/quads/faq"
        class:list={[
          "hidden h-9 items-center gap-1.5 rounded-sm border px-3 text-[0.85rem] font-medium transition-colors md:inline-flex",
          isActive("/quads/faq")
            ? "border-primary text-primary"
            : "border-border text-muted-foreground hover:border-input hover:text-foreground",
        ]}
      >
        <svg
          width="14"
          height="14"
          viewBox="0 0 24 24"
          fill="none"
          stroke="currentColor"
          stroke-width="2"
          stroke-linecap="round"
          stroke-linejoin="round"
          aria-hidden="true"
        >
          <circle cx="12" cy="12" r="10" />
          <path d="M9.1 9a3 3 0 0 1 5.8 1c0 2-3 3-3 3" />
          <path d="M12 17h.01" />
        </svg>
        Quad FAQ
      </a>
      <a
        href={nextQuad.register ?? "/quads"}
        target={nextQuad.register ? "_blank" : undefined}
        rel={nextQuad.register ? "noopener noreferrer" : undefined}
        class="hidden h-9 items-center gap-1.5 rounded-sm bg-primary px-4 text-[0.88rem] font-medium text-primary-foreground transition-colors hover:bg-primary/90 sm:inline-flex"
      >
        <Bolt size={13} />
        Register for a quad
      </a>
      <MobileNav />
    </div>
  </div>
</header>
